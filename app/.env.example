# PromptHub Router Configuration
# Copy this to .env and customize

# Server
HOST=127.0.0.1
PORT=9090

# Local LLM server (LM Studio, Ollama, or any OpenAI-compatible server)
LLM_HOST=localhost
LLM_PORT=1234
LLM_MODEL=qwen3-4b-instruct-2507
LLM_ORCHESTRATOR_MODEL=qwen3-4b-thinking-2507
LLM_TIMEOUT=120
# Legacy aliases (still work): OLLAMA_HOST, OLLAMA_PORT, OLLAMA_MODEL, OLLAMA_TIMEOUT

# Data directory — persistent storage for cache, activity log, memory db
# Defaults to ~/.prompthub (XDG-style user data dir, survives reboots)
DATA_DIR=~/.prompthub

# Cache
CACHE_MAX_SIZE=1000
# Persistent L2 cache (SQLite write-through, survives restarts)
CACHE_PERSISTENT=false
# Individual DB/log paths — all default to $DATA_DIR/<name> if not set
# CACHE_DB_PATH=~/.prompthub/cache.db
# ACTIVITY_DB_PATH=~/.prompthub/activity.db
# MEMORY_DB_PATH=~/.prompthub/memory.db
# TOOL_REGISTRY_DB_PATH=~/.prompthub/tool_registry.db
# AUDIT_LOG_PATH=~/.prompthub/audit.log
# AUDIT_CHECKSUM_PATH=~/.prompthub/audit_checksums.json

# Circuit Breaker
CB_FAILURE_THRESHOLD=3
CB_RECOVERY_TIMEOUT=30

# Paths
MCP_SERVERS_CONFIG=configs/mcp-servers.json
ENHANCEMENT_RULES_CONFIG=configs/enhancement-rules.json
# OpenAI proxy bearer tokens (always active at /v1/ endpoints)
API_KEYS_CONFIG=configs/api-keys.json

# OpenRouter (cloud fallback for prompt enhancement)
# When LLM server is down, clients with privacy_level "free_ok" or "any" fall back to OpenRouter.
# Clients with "local_only" (the default) never use cloud fallback.
# OPENROUTER_API_KEY is resolved from keyring if not set here.
# Store with: python scripts/security/manage-keys.py set openrouter_api_key
OPENROUTER_ENABLED=false
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_TIMEOUT=30
OPENROUTER_DEFAULT_MODEL=qwen/qwen3-next-80b-a3b-instruct:free

# Logging
LOG_LEVEL=info

# Secrets — stored in macOS Keychain via keyring (service="prompthub")
# Manage with: python scripts/security/manage-keys.py list
# Keys: openrouter_api_key, obsidian_api_key, obsidian_authentication, hf_api_key
